export default function App() {
  return (
    <main className="flex min-h-svh items-center justify-center bg-slate-50 px-4 py-8 sm:px-6 lg:px-8">
      <section className="w-full max-w-lg rounded-xl border border-slate-200 bg-white p-6 shadow-sm sm:p-8">
        <p className="text-sm font-medium text-slate-500">Field Service System</p>
        <h1 className="mt-2 text-2xl font-semibold tracking-tight text-slate-900 sm:text-3xl">
          Frontend foundation configured
        </h1>
        <p className="mt-3 text-sm leading-6 text-slate-600 sm:text-base">
          React, TypeScript, Tailwind CSS, Axios, and React Router are ready for
          the next implementation step.
        </p>
      </section>
    </main>
  );
}